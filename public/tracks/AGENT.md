# Tracks Folder Guide

Purpose

- `route.json`: the race-course track data rendered by the Map tab
  (`race/[id]/map/Map.tsx` and `components/map/RaceMap.tsx`).

Read Order

1. ../../AGENT.md
2. ../AGENT.md

No child folders.
